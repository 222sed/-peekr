# Peekr 产品需求文档

版本：V2.0 · 定位重整稿  
日期：2026-09-07  
依据：用户定位访谈、2026-06-22 版 Word PRD、此前提供的 README 与已确认首页效果图。本文描述目标产品及验证计划，不代表所有能力已实现。

## 1. 产品定位

Peekr 面向家中养一只猫、已有摄像头、白天外出的主人，将摄像头观察到的猫咪近况转化为手机桌面上的动态宠物形象与行为状态条。主人无需特意打开监控，即可顺手了解近况；有疑问时查看最近画面或进入原监控 App 确认。

产品解决两个问题：主动打开监控存在操作负担；忙碌时容易忘记查看。动态猫咪提供陪伴感，状态、时间和行为记录提供事实依据。安心是希望通过用户验证取得的体验结果，目前不是已证实效果。

长期产品接入现有摄像头，目标是配置一次、尽量少维护。旧手机是原型采集方案，不是长期产品的核心卖点。

## 2. 已确认决策

| 事项 | 决策 |
| --- | --- |
| 初始用户 | 单猫家庭、已有摄像头、白天外出的主人 |
| 主要入口 | 手机桌面组件，小程序或配套客户端承接详情与设置 |
| 角色表达 | 用户偏好动画；根据真实观察结果切换行为 |
| 可接受近况时效 | 最长半小时；不等于故意每半小时才更新 |
| 离镜表现 | 保留带明确时间的最近文字记录，不继续声称实时行为 |
| 核心指标 | 进食、睡眠、活动；睡眠在识别与文案中标记为推测休息 |
| 状态条目的 | 帮助理解记录偏少、参考范围内或偏多，不是越满越好 |
| 范围来源 | 以年龄、体重相关群体参考作为起点，随有效历史逐步建立个体基线 |
| 饭盆 | 位置固定，需配置食盆区域并验证镜头确实覆盖 |
| 提醒原则 | 安静展示为主；优先明确可验证的设备问题，行为提醒另行验证 |
| 深入确认 | 可查看最近画面；必要时进入监控找猫、手动转动摄像头 |

## 3. 产品范围与实现状态

### 3.1 当前原型证据

旧 Word PRD 描述了微信双小程序、FastAPI 后端、YOLOv8n、动物姿态与时序规则的基础闭环。此前 README 还描述了 SQLite 历史、食盆配置和空满碗校准。两份材料在上传间隔、数据存储、食盆功能阶段等方面存在版本差异，需对实际运行版本核验后再更新实现清单。

当前工作目录尚无应用源码，因此本文不将旧文档中的实现状态视为本地代码验证结果。旧文档中的准确率、延迟、成功率数字属于目标，不作为实测成绩。

### 3.2 目标产品新增能力

- 原生桌面组件或等效桌面入口。
- 现有摄像头接入及最近画面查询。
- 年龄、体重等档案信息及可追溯参考范围。
- 按数据质量控制的个体基线学习。
- 三项行为状态条及其判断说明。

上述能力均为规划或待验证。小程序本身不作为已经实现系统桌面组件的证据。平台选择、动画和刷新约束需要独立技术验证。

### 3.3 首版不包含

多猫身份识别、疾病诊断、健康总分、情绪识别、可靠饮水量测量、所有品牌摄像头通用接入、自动云台追踪，以及未经验证的行为风险推送。

## 4. 核心体验与页面架构

### 4.1 桌面组件

核心内容是动态猫咪、最近状态、观察时间，以及进食、休息、活动三个状态条。画面采用已确认的暖白、炭黑、珊瑚红配色和原始品牌猫咪。桌面空间优先保证文字与数据可读；大型圆弧和完整图表可留在详情首页。

桌面组件显示最近可用的数据快照，不要求用户理解识别技术。点击整体进入详情，平台支持时为单项指标提供直接入口。

### 4.2 配套详情端

| 页面 | 核心内容 |
| --- | --- |
| 陪伴 | 中央猫咪、实时或最近状态、更新时间、圆弧今日概况、三项状态条、查看最近画面 |
| 日常 | 日期、有效记录范围、状态时间轴、进食事件、参考范围与个体历史比较 |
| 设备 | 摄像头连接、最后采集、画面质量、机位与食盆区域、重新校准 |
| 设置入口 | 猫咪档案与外观、动效、通知、数据管理、演示模式 |

圆弧可表达记录覆盖率，不能默认表示健康分。覆盖率为有效记录时长除以当日已经过去的时长，需注明观察开始时间和缺失区间。日内统计仅与相同时段或已完成的可比窗口比较，不直接对照全天目标。

### 4.3 首次使用

创建猫咪档案 → 检查摄像头兼容性 → 授权并连接 → 选择固定观察机位 → 标记固定饭盆区域 → 预览首次识别 → 添加桌面组件。

猫咪年龄和体重为参考资料必填项；体重需附测量日期，年龄允许估计并标识不确定。绝育状态、喂养方式、主要粮食及已知特殊照护需求按所采用参考资料的要求采集。资料不完整时允许记录，但不强行输出范围判断。

## 5. 观察状态与新鲜度

数据层分别保存采集时间、推理完成时间、客户端拉取时间、最近发现猫咪时间。界面的“多久前观察”使用采集时间，不能以拉取成功时间冒充最新观察。

| 情况 | 表现 |
| --- | --- |
| 有持续可靠观察 | 对应行为动画、状态文字、观察时间 |
| 已确认猫咪离开画面 | 保留最近文字记录及时间；停止表示实时活动、进食或休息的动作 |
| 最后有效观察超过30分钟 | “暂无近期记录”，同时保留最后文字记录；不因离镜自动推送 |
| 图像可用但无法判断行为 | “暂时无法确认状态”，不强行归入休息 |
| 设备未上传 | 明确采集端离线，展示最近成功采集时间和处理入口 |
| 服务或客户端网络故障 | 单独提示连接问题，不能直接断言摄像头离线 |
| 首次暂无数据 | 等待首次记录，给出设备检查入口 |
| 演示模式 | 组件及详情均持续标识演示数据 |

动画呈现不能超过证据新鲜度。若平台不能及时更新组件，不承诺实时动画同步；需显示快照时间。离镜后的角色可保留静态形象，最后文字记录为必需内容。

## 6. 行为记录与固定饭盆

### 6.1 进食

通过猫咪检测、头鼻部与食盆区域关系、姿态和连续时序证据识别推测进食事件。进入饭盆区域不等于进食，需区分经过、嗅闻、舔毛和实际持续进食。

事件包含起止时间、观察质量、区域版本和是否经用户纠正。短暂离开后重新进入的事件合并规则，通过实拍测试确定，不在本文虚构固定阈值。

首版记录推测进食次数、持续时长和发生时间。次数和持续时长不直接换算摄入克数、热量或饱腹程度。若后续获得称重、可靠余量差或用户投喂记录，需建立独立的摄入量指标及验证流程。

固定饭盆减少区域歧义，但不保证摄像头始终对准饭盆。用户手动转动云台后，原画面坐标可能失效；机位变化时暂停区域判断并提示恢复预设机位或重新校准。食盆区域被遮挡、过暗或画面丢失时，不将没有进食事件解释为未进食。

### 6.2 休息与活动

休息依据静止、身体形态与时序证据推测，不能确认生理睡眠。产品导航可解释为睡眠相关记录，数值使用“推测休息”或“约”。活动记录运动行为，不把所有移动解释为玩耍。

连续状态用于时间轴；进食事件单独叠加。允许进食事件与活动证据重叠，统计不得将不同口径简单相加当作一天总时长。低活动、无法判断、未入镜、未采集分别记录。

## 7. 参考范围与个体学习

### 7.1 核心原则

采用“群体参考＋个体历史”的双层机制。年龄、体重用于匹配适用的群体参考，但不能单独确定所有猫咪行为的健康正常值。群体平均值也不等于正常范围，参考必须包含指标定义、单位、适用人群、区间、来源与版本。

群体参考与个体基线在产品和数据层分开保存、分开解释。无适用资料时展示实际记录与学习状态，不生成看似科学的上下限。

### 7.2 各指标的判断依据

| 指标 | 群体参考用途 | 个体历史用途 | 限制 |
| --- | --- | --- | --- |
| 推测进食次数与时长 | 仅使用匹配年龄、喂养方式、同一测量口径的可靠行为资料 | 比较同一只猫、相同时段、固定饭盆可观察条件下的记录 | 不与热量或克数推荐值直接比较 |
| 推测休息 | 有适用且同口径资料时提供参考，否则仅记录 | 比较相同时段与相近覆盖条件下的休息分布 | 局部机位观察不是全天真实睡眠 |
| 活动 | 需要与当前算法、单位、观察方式一致的参考 | 比较个人活动时长及日内分布 | 模型活动分不能直接套用通用运动推荐 |

年龄体重档案驱动群体先验的选择；个体数据积累后提高个人比较的作用。不同单位、不同含义的参考不做加权混合。

### 7.3 学习流程

1. 初始阶段：建立档案，展示可用参考及来源；若缺少同口径参考，标注“正在了解它的日常”。
2. 数据筛选：仅纳入采集有效、区域有效、无严重遮挡且时段可比的记录。缺失数据不补零。
3. 建立基线：采用可解释的滚动历史统计与区间，先验证稳定性和所需样本量，再确定有效天数、窗口和更新频率。
4. 持续更新：按可配置周期更新，而非每一帧重写基线；保存版本和依据。
5. 防止错误学习：突发偏离、机位变化、算法变化、用户标记的特殊日不自动吸收入日常基线；持续变化需复核，不能无限把偏离重新定义为正常。
6. 档案变化：体重、年龄阶段或喂养方式改变时提示重新评估基线，不清除原始历史。

“持续学习”首阶段指个体统计基线更新，不宣称自动训练视觉模型。行为模型训练是独立研发事项。

### 7.4 判断顺序

先判断数据是否足够 → 再判断参考是否适用 → 再判断比较时段是否一致 → 最后输出偏低、区间内或偏高。

有效数据门槛需按指标分别定义。饭盆持续可见时可以形成较完整的进食观察，但这不能证明全屋活动和睡眠也记录完整。

## 8. 状态条及说明

状态条保留电子宠物的视觉趣味，采用“偏少—参考区间—偏多”的轨道和当前位置标记。满格不表示优秀，不采用越高越健康的逻辑。

每项显示名称、实际值、比较结果和可点击说明。详情说明比较的是群体参考还是个人历史，覆盖哪个时段，记录是否完整，来源或基线版本是什么。

可用状态包括：低于参考、参考区间内、高于参考、正在学习、记录不足、参考暂不可用。界面优先用“接近平时”“低于平时”等精确措辞；“正常范围”仅作为用户需求名称，不等于医学结论。

例：进食“推测3次 · 接近平时”；休息“约5小时 · 记录不足，暂不比较”。示例数字仅供交互设计，不能作为默认标准。

## 9. 最近画面与监控衔接

点击状态或查看入口，展示最近可用画面及其拍摄时间。旧画面明确标注，不显示为直播。允许进入原摄像头 App；跳转、直播、云台控制是否可用取决于设备接口与平台验证。

Peekr 首版不必重建完整监控控制台。无可用跳转能力时明确提供操作指引，不能放置没有响应的入口。账号凭据采用授权机制和安全存储，不要求用户将密码写入代码。

## 10. 通知策略

桌面安静展示为默认。设备离线仅在故障持续且原因可确认时提醒，支持用户关闭、去重和恢复提示；持续阈值及重发间隔待测试确定。

猫咪离镜、单次识别失败、单日状态条偏离不默认推送。行为提醒需验证判断依据、误报率、数据完整度和可行动性后单独开放，并取得用户选择。

## 11. 数据与隐私

保存猫咪档案、观察快照、行为区段、进食事件、画面质量、设备状态、区域校准、参考来源及个体基线版本。

字段需支持采集时间、时区、数据来源、演示标识、缺失原因及用户纠正。重复上传不得重复累计事件或时长。跨日记录按一致时区切分。

用户应能停止采集、撤销设备连接并删除记录。最近画面采用最少必要留存，具体期限和存储位置在架构阶段确定；不默认保存完整家庭录像。抽象猫咪展示不等于图像不离设备。

## 12. 优先级与阶段计划

| 阶段 | 主要工作 | 完成依据 |
| --- | --- | --- |
| A 定位与交互验证 | 桌面组件原型、三项状态条、离镜文字记录、最近画面入口 | 用户能说清状态、时间、条形含义及查看路径 |
| B 技术可行性 | 选定一款实际摄像头和一个目标移动平台，验证授权、取图、桌面刷新和动画 | 实机运行证据；公布适配范围与限制 |
| C 观察链路 | 固定饭盆、机位变化处理、时间戳、缺失记录、历史存储 | 实拍场景测试与人工标注对照 |
| D 范围与学习 | 筛选可用参考资料、建立个体基线、状态条说明与质量门槛 | 同口径比较可追溯，缺数据时不错误判断 |
| E 小规模试用 | 单猫家庭、现有摄像头、低维护运行 | 查看负担、信任、误报、维护成本与保留意愿的真实反馈 |

摄像头接入与桌面实现是长期定位的前置验证，不应在大量视觉制作完成后才发现不可行。旧手机链路可以继续支撑行为识别试验。

## 13. 验收与评测

- 用户能在约5秒内找到最近状态与观察时间；该值为待验证设计目标。
- 离镜、摄像头离线、网络故障和识别不确定可被正确区分。
- 超过30分钟无有效观察，不将旧状态显示为近期实时信息。
- 猫咪离镜后保留文字记录，不继续播放暗示当前行为的动画。
- 三项状态条均显示实际值、比较依据或无法判断原因。
- 没有事件但饭盆不可见时，不输出进食偏少；没有活动画面时，不输出全天不动。
- 镜头转动使区域失效时，暂停依赖该区域的判断，提供恢复步骤。
- 参考资料单位与产品指标不匹配时，不能输出范围判断。
- 个人基线更新可追溯，异常记录不悄悄改变通常范围。
- 测试覆盖白天、夜间、遮挡、离镜、断网、云台移动、饭盆嗅闻与真实进食。
- 行为识别以人工标注对照评估各类准确性、误判和响应延迟；不预先宣称结果。
- 用户试用记录主动打开监控的原因、查看耗时、组件保留意愿、维护次数与错误提醒；监控打开次数降低不单独作为成功标准。

## 14. 待验证事项

本轮用户决策已结束，以下为研发和研究验证事项，不将其默认为已解决：摄像头品牌型号和接口、首个平台、组件动效与刷新能力；年龄体重对应的同口径行为参考是否存在；不同指标所需有效样本量、范围上下限与基线更新策略；画面留存和故障提醒参数。

当前产品承诺是降低获取近况的负担，并提供有依据的行为变化提示。不能将局部摄像头观察概括为对猫咪全天安全或健康的保证。

## 15. 参考资料

用户提供：Peekr-PRD.docx（V1.0，2026-06-22）、此前 README、已确认界面效果图，以及本轮定位访谈。

营养参考的使用边界：AAHA 的猫咪营养指南指出，营养评估涉及年龄、绝育状态、体况、肌肉状况、活动及疾病等因素。因此年龄和体重可作为起点，但本文未从这些资料推导进食次数、活动分钟数或睡眠时长的正常值。

- AAHA，Nutrition and Weight Management：https://www.aaha.org/resources/2021-aaha-aafp-feline-life-stage-guidelines/nutrition-and-weight-management-2/
- AAHA，How to Perform a Complete Nutritional Assessment：https://www.aaha.org/resources/2021-aaha-nutrition-and-weight-management-guidelines/how-to-nutritional-assessment/
- WSAVA，Nutrition Committee：https://wsava.org/committees/global-nutrition-committee/
